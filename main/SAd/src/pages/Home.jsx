import { spaInfo, features } from '../data/spaData';
import Hero from '../components/Hero';

const Home = () => {
  return (
    <div id="home">
      {/* Hero Section */}
      <Hero />
    </div>
  );
};

export default Home;
